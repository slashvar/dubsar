class foo

class bar {}

class foobar {
	/* dtor */
	delete {
		x = -1;
	}
	/* the rest */
	var x = 0;
	/* just */
	fun getX() {
		return x;
	}
	/* end */
}

class <T> Box(v : T) {
	var x : T = v;
	fun get() {
		return x;
	}
	fun set(v) {
		x = v;
	}
}